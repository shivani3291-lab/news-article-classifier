name: Keep Streamlit App Awake

on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping app
        run: curl -sS -o /dev/null -w "%{http_code}" https://news-article-classifier-eyd497t5avdhywvkmqbzu3.streamlit.app/
